public class CharacterMethods {
    public static void main(String[] args) {
        char c1 = 'a';
        System.out.println(c1);

        // Character Methods:
        // Character.toString(ch) - returns the string representation of the character or String.valueOf()
        // Character.isDigit(c1) - returns true if the character is a digit
        // Character.isAlphabetic(c1) - returns true if the character is alphabetic
        // Character.isUpperCase(c1) - returns true if the character is uppercase
        // Character.isLowerCase(c1) - returns true if the character is lowercase
        // Character.isWhitespace(c1) - returns true if the character is whitespace
        // Character.isLetter(c1) - returns true if the character is a letter
        // Character.isLetterOrDigit(c1) - returns true if the character is a letter or digit
        // Character.isSpaceChar(c1) - returns true if the character is a space character
        // Character.toUpperCase(c1) - returns the uppercase equivalent of the character
        // Character.toLowerCase(c1) - returns the lowercase equivalent of the character
        // Character.isJavaIdentifierStart('a') - returns true if the character is a valid Java identifier start character
        // Character.isJavaIdentifierPart('a') - returns true if the character is a valid Java identifier part character
        // Character.compare('a', 'b'); // negative
        // Character.compare('b', 'a'); // positive
        // Character.compare('a', 'a'); // 0
        // char ch = Character.forDigit(5, 10); - returns the character equivalent of the digit in the given radix
    }
}